apiVersion: v1
kind: Service
metadata:
  name: {{ include "vendor-svc.fullname" . }}
  labels:
    {{- include "vendor-svc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vendor-svc.selectorLabels" . | nindent 4 }}
